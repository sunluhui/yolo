# UAVDT数据集配置示例
path: /home/a10/slh/yolo/datasets/OpenDataLab___UAVDT/sample  # 数据集根目录（绝对路径或相对路径）
train: images/train     # 训练集图片路径（相对path的路径）
val:
test: images/test     # 测试集图片路径（可选）

nc: 3                    # 类别总数
names:              # 类别名称列表（顺序需与标签索引一致）
  0: car
  1: truck
  2: bus