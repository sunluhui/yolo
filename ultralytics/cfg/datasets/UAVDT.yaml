# UAVDT数据集配置示例
path: /home/a10/ZH/ultralytics-main/datasets/UAVDT  # 数据集根目录（绝对路径或相对路径）
train: train/images      # 训练集图片路径（相对path的路径）
test: test/images       # 测试集图片路径（可选）

nc: 3                    # 类别总数
names:              # 类别名称列表（顺序需与标签索引一致）
  0: car
  1: bus
  2: truck