# TinyPerson Dataset YAML Configuration
path: /home/a10/slh/yolo/datasets/TinyPerson  # 数据集根目录
train: images/train        # 训练集相对路径
val: images/val            # 验证集相对路径
test: images/test          # 测试集相对路径（可选）

# 类别定义
names:
  0: person

# 类别数量
nc: 1