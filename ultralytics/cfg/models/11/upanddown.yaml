# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# Enhanced RT-DETR-ResNet50 for Small Object Detection
# Integrated with Haar Wavelet Downsampling (HWD) and CARAFE Upsampling
# Task docs: https://docs.ultralytics.com/tasks/detect
# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants
  # [depth, width, max_channels]
  l: [1.00, 1.00, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2

  # 替换MaxPool为HWD小波下采样 - 保留高频信息
  - [-1, 1, HWD, [64]] # 3-P2/4 (替换原来的MaxPool)

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4

  # 在关键下采样点使用HWD
  - [-1, 1, HWD, [128]] # 5-P3/8 (新增HWD下采样)
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 6

  - [-1, 1, HWD, [256]] # 7-P4/16 (新增HWD下采样)
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 8

  - [-1, 1, HWD, [512]] # 9-P5/32 (新增HWD下采样)
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 10

head:
  - [-1, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 11 input_proj.2
  - [-1, 1, AIFI, [1024, 8]] # 12
  - [-1, 1, Conv, [256, 1, 1]]  # 13, Y5, lateral_convs.0

  # 替换最近邻上采样为CARAFE内容感知上采样
  - [-1, 1, CARAFE, [256]] # 14 (替换nn.Upsample)

  - [8, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 15 input_proj.1
  - [[-2, -1], 1, Concat, [1]] # 16
  - [-1, 3, RepC3, [256, 0.5]]  # 17, fpn_blocks.0
  - [-1, 1, Conv, [256, 1, 1]]   # 18, Y4, lateral_convs.1

  # 第二个CARAFE上采样
  - [-1, 1, CARAFE, [256]] # 19 (替换nn.Upsample)

  - [6, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 20 input_proj.0
  - [[-2, -1], 1, Concat, [1]]  # 21 cat backbone P3
  - [-1, 3, RepC3, [256, 0.5]]    # X3 (22), fpn_blocks.1

  - [-1, 1, Conv, [256, 3, 2]]   # 23, downsample_convs.0
  - [[-1, 18], 1, Concat, [1]]  # 24 cat Y4
  - [-1, 3, RepC3, [256, 0.5]]    # F4 (25), pan_blocks.0

  - [-1, 1, Conv, [256, 3, 2]]   # 26, downsample_convs.1
  - [[-1, 13], 1, Concat, [1]]  # 27 cat Y5
  - [-1, 3, RepC3, [256, 0.5]]    # F5 (26), pan_blocks.1

  - [[22, 25, 26], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]]  # Detect(P3, P4, P5)