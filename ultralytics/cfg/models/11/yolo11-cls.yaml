# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11 object detection model with P3/8 - P5/32 outputs
# Model docs: https://docs.ultralytics.com/models/yolo11
# Task docs: https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n.yaml' will call yolo11.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # summary: 319 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # summary: 319 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512] # summary: 409 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512] # summary: 631 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512] # summary: 631 layers, 56966176 parameters, 56966160 gradients, 196.0 GFLOPs

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, CA_RFA_SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

# YOLO11n head
head:
  # 首先从backbone提取特征图并调整通道数
  - [4, 1, Conv, [head_channel]]  # 11-P3/8 (来自backbone第4层)
  - [6, 1, Conv, [head_channel]]  # 12-P4/16 (来自backbone第6层)
  - [10, 1, Conv, [head_channel]] # 13-P5/32 (来自backbone第10层)

  # BiFPN 上采样路径 P5 -> P4
  - [-1, 1, CARAFE, []] # 14 P5->P4 上采样
  - [[-1, 12], 1, Fusion, [fusion_mode]] # 15 融合P5上采样和P4
  - [-1, 2, node_mode, [head_channel, False]] # 16-P4/16

  # BiFPN 上采样路径 P4 -> P3
  - [-1, 1, CARAFE, []] # 17 P4->P3 上采样
  - [[-1, 11], 1, Fusion, [fusion_mode]] # 18 融合P4上采样和P3
  - [-1, 2, node_mode, [head_channel, False]] # 19-P3/8

  # BiFPN 下采样路径 P3 -> P4
  - [-1, 1, Conv, [head_channel, 3, 2]] # 20 P3->P4 下采样
  - [[-1, 11, 19], 1, Fusion, [fusion_mode]] # 21 融合P3下采样、原始P3和P3特征
  - [-1, 2, node_mode, [head_channel, False]] # 22-P4/16

  # BiFPN 下采样路径 P4 -> P5
  - [-1, 1, Conv, [head_channel, 3, 2]] # 23 P4->P5 下采样
  - [[-1, 12, 16], 1, Fusion, [fusion_mode]] # 24 融合P4下采样、原始P4和P4特征
  - [-1, 2, node_mode, [head_channel, True]] # 25-P5/32

  # 检测头
  - [[19, 22, 25], 1, Detect, [nc]]  # Detect(P3, P4, P5)
