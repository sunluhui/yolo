# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics RT-DETR-ResNet50 hybrid object detection model with P2/4 - P5/32 outputs
# Enhanced with P2-P3 feature fusion for small object detection
# Model docs: https://docs.ultralytics.com/models/rtdetr
# Task docs: https://docs.ultralytics.com/tasks/detect
# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolov8n-cls.yaml' will call yolov8-cls.yaml with scale 'n'
  # [depth, width, max_channels]
  l: [1.00, 1.00, 1024]
backbone:
  # [from, repeats, module, args]
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2
  - [-1, 1, HWD, [64, 64, 3, 2, 1, 1, 'relu']]#- [-1, 1, nn.MaxPool2d, [3, 2, 1]] # 3-P2/4

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4-P2/4 (small object rich layer)
  - [-1, 1, BiFormerBlock, [64]]
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 5-P3/8
  - [-1, 1, BiFormerBlock, [128]]
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 6-P4/16
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 7-P5/32--9

head:
  # Enhanced P2-P3 fusion for small object detection (重点加强小目标检测路径)
  - [5, 1, Conv, [128, 1, 1, None, 1, 1, False]]  # 8, P2 lateral projection10
  - [7, 1, Conv, [128, 1, 1, None, 1, 1, False]]  # 9, P3 lateral projection11

  # P2-P3 fusion with enhanced processing for small objects
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 10, upsample P3 to match P2 spatial size12
  - [[10, -1], 1, Concat, [1]] # 11, concat P2 and upsampled P313
  - [-1, 3, RepC3, [256, 0.5]]  # 12, fused P2-P3 features (保持3次重复，加强小目标特征)14

  # Downsample fused features to P3 size
  - [-1, 1, Conv, [256, 3, 2]]   # 13, downsample to P3/815
  - [[11, -1], 1, Concat, [1]] # 14, concat with original P316
  - [-1, 3, RepC3, [256, 0.5]]  # 15, enhanced P3 features (保持3次重复)17

  # Enhanced P3 output for small objects
  - [-1, 1, Conv, [256, 1, 1]]  # 16, Y3 enhanced lateral18

  # Upper pyramid processing (减少大目标路径的复杂度)
  - [9, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 17 input_proj.19
  - [-1, 1, AIFI, [1024, 8]] # 18- [-1, 1, SBSAtt, []]20
  - [-1, 1, Conv, [256, 1, 1]]  # 19, Y5, lateral_convs.021

  # P5 to P4 (减少重复次数)
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 20
  - [8, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 21 input_proj.1
  - [[-2, -1], 1, Concat, [1]] # 22
  - [-1, 1, RepC3, [256, 0.5]]  # 23, fpn_blocks.0 (减少到1次重复)
  - [-1, 1, Conv, [256, 1, 1]]   # 24, Y4, lateral_convs.1

  # P4 to P3 (连接到增强的P3特征)
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 25
  - [[18, -1], 1, Concat, [1]]  # 26 cat enhanced P3
  - [-1, 1, RepC3, [256, 0.5]]    # X3 (27), fpn_blocks.1 (减少到1次重复)
  - [-1, 1, BiFormerBlock, [256]]

  # Bottom-up path (PAN) - 减少重复次数
  - [-1, 1, Conv, [256, 3, 2]]   # 28, downsample_convs.0
  - [[-1, 26], 1, Concat, [1]]  # 29 cat Y4
  - [-1, 1, RepC3, [256, 0.5]]    # F4 (30), pan_blocks.0 (减少到1次重复)
  - [-1, 1, BiFormerBlock, [256]]

  - [-1, 1, Conv, [256, 3, 2]]   # 31, downsample_convs.1
  - [[-1, 21], 1, Concat, [1]]  # 32 cat Y5
  - [-1, 1, RepC3, [256, 0.5]]    # F5 (33), pan_blocks.1 (减少到1次重复)

  - [[30, 34, 37], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]]  # Detect(enhanced P3, P4, P5)