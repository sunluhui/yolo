# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics RT-DETR-ResNet50 with BiLevelRoutingAttention for small object detection
# Parameters
nc: 80 # number of classes
scales:
  l: [1.00, 1.00, 1024]
backbone:
  # [from, repeats, module, args]
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2
  - [-1, 1, nn.MaxPool2d, [3, 2, 1]] # 3-P2/4

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 5-P3/8
  - [-1, 1, BiFormerBlock, [128]]  # 6 新增：在P3层添加注意力，增强小目标特征
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 7-P4/16
  - [-1, 1, BiFormerBlock, [256]]  # 8 新增：在P4层添加注意力
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 9-P5/32

head:
  - [-1, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 10 input_proj.2
  - [-1, 1, AIFI, [1024, 8]] # 11
  - [-1, 1, Conv, [256, 1, 1]]  # 12, Y5, lateral_convs.0

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 13
  - [8, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 14 input_proj.1 (来自backbone第7层)
  - [[-2, -1], 1, Concat, [1]] # 15
  - [-1, 3, RepC3, [256, 0.5]]  # 16, fpn_blocks.0
  - [-1, 1, BiFormerBlock, [256]]  # 17 新增：在FPN路径添加注意力
  - [-1, 1, Conv, [256, 1, 1]]   # 18, Y4, lateral_convs.1

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 19
  - [6, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 20 input_proj.0 (来自backbone第5层)
  - [[-2, -1], 1, Concat, [1]]  # 21 cat backbone P3
  - [-1, 3, RepC3, [256, 0.5]]    # 22, fpn_blocks.1
  - [-1, 1, BiFormerBlock, [256]]  # 23 新增：在最高分辨率层加强小目标检测

  - [-1, 1, Conv, [256, 3, 2]]   # 24, downsample_convs.0
  - [[-1, 18], 1, Concat, [1]]  # 25 cat Y4 (来自第18层)
  - [-1, 3, RepC3, [256, 0.5]]    # 26, pan_blocks.0

  - [-1, 1, Conv, [256, 3, 2]]   # 27, downsample_convs.1
  - [[-1, 12], 1, Concat, [1]]  # 28 cat Y5 (来自第12层)
  - [-1, 3, RepC3, [256, 0.5]]    # 29, pan_blocks.1

  - [[23, 26, 29], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]]  # Detect(P3, P4, P5)