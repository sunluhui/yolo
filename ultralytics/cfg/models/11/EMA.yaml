# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# Enhanced RT-DETR-ResNet50 with BiFormerBlock for Small Object Detection
# Model docs: https://docs.ultralytics.com/models/rtdetr
# Task docs: https://docs.ultralytics.com/tasks/detect
# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants
  l: [1.00, 1.00, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2
  - [-1, 1, nn.MaxPool2d, [3, 2, 1]] # 3-P2/4

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 5-P3/8
  # 🔥 在P3层后添加BiFormerBlock - 增强小目标基础特征提取 [citation:2][citation:10]
  - [-1, 1, BiFormerBlock, [128, 4, 7, 2]] # 6-P3/8-BiFormer (dim=128, heads=4, n_win=7, topk=2)

  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 7-P4/16
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 8-P5/32

head:
  - [-1, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 9 input_proj.2
  - [-1, 1, AIFI, [1024, 8]] # 10
  - [-1, 1, Conv, [256, 1, 1]]  # 11, Y5, lateral_convs.0

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 12
  - [7, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 13 input_proj.1
  - [[-2, -1], 1, Concat, [1]] # 14
  - [-1, 3, RepC3, [256, 0.5]]  # 15, fpn_blocks.0
  - [-1, 1, Conv, [256, 1, 1]]   # 16, Y4, lateral_convs.1

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']] # 17
  - [6, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 18 input_proj.0 (注意索引指向P3层的BiFormer输出)
  - [[-2, -1], 1, Concat, [1]]  # 19 cat backbone P3
  # 🔥 在FPN高层特征融合后添加BiFormerBlock - 优化多尺度特征融合 [citation:6]
  - [-1, 1, BiFormerBlock, [256, 8, 7, 4]] # 20-FPN-BiFormer (dim=256, heads=8, n_win=7, topk=4)

  - [-1, 3, RepC3, [256, 0.5]]    # 21, fpn_blocks.1

  - [-1, 1, Conv, [256, 3, 2]]   # 22, downsample_convs.0
  - [[-1, 16], 1, Concat, [1]]  # 23 cat Y4
  - [-1, 3, RepC3, [256, 0.5]]    # 24, pan_blocks.0

  - [-1, 1, Conv, [256, 3, 2]]   # 25, downsample_convs.1
  - [[-1, 11], 1, Concat, [1]]  # 26 cat Y5
  # 🔥 在PAN路径底层特征融合后添加BiFormerBlock - 增强大感受野特征 [citation:9]
  - [-1, 1, BiFormerBlock, [256, 8, 7, 4]] # 27-PAN-BiFormer (dim=256, heads=8, n_win=7, topk=4)
  - [-1, 3, RepC3, [256, 0.5]]    # 28, pan_blocks.1

  # 检测层索引更新为新的层编号
  - [[21, 24, 28], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]]  # Detect(P3, P4, P5)