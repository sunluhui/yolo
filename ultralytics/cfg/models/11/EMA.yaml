# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license
# Enhanced RT-DETR-ResNet50 for Small Object Detection
# Integrated with:
# 1. Haar Wavelet Downsampling (HWD)
# 2. CARAFE Upsampling
# 3. Efficient Multi-Scale Attention (EMA)
# Task docs: https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants
  l: [1.00, 1.00, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, ConvNormLayer, [32, 3, 2, None, False, 'relu']] # 0-P1/2
  - [-1, 1, ConvNormLayer, [32, 3, 1, None, False, 'relu']] # 1
  - [-1, 1, ConvNormLayer, [64, 3, 1, None, False, 'relu']] # 2

  # 🔥 替换MaxPool为HWD小波下采样 - 保留高频信息
  - [-1, 1, HWD, [64]] # 3-P2/4 (替换原来的MaxPool)

  - [-1, 1, Blocks, [64, BasicBlock, 2, 2, 'relu']] # 4

  # 🔥 在关键下采样点使用HWD + 添加EMA注意力
  - [-1, 1, HWD, [128]] # 5-P3/8 (HWD下采样)
  - [-1, 1, Blocks, [128, BasicBlock, 2, 3, 'relu']] # 6
  # 🔥 在backbone的P3层添加EMA - 直接增强小目标原始特征
  - [-1, 1, MLKA, [128]] # 7-P3多尺度增强

  - [-1, 1, HWD, [256]] # 8-P4/16 (HWD下采样)
  - [-1, 1, Blocks, [256, BasicBlock, 2, 4, 'relu']] # 9
  # 🔥 在backbone的P4层添加EMA - 增强中层特征
  - [-1, 1, MLKA, [256]] # 10-P4多尺度增强

  - [-1, 1, HWD, [512]] # 11-P5/32 (HWD下采样)
  - [-1, 1, Blocks, [512, BasicBlock, 2, 5, 'relu']] # 12

head:
  - [-1, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 13 input_proj.2
  - [-1, 1, AIFI, [1024, 8]] # 14
  - [-1, 1, Conv, [256, 1, 1]]  # 15, Y5, lateral_convs.0

  # 🔥 替换最近邻上采样为CARAFE内容感知上采样
  - [-1, 1, CARAFE, [256]] # 16 (替换nn.Upsample)

  - [9, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 17 input_proj.1 (来自P4层)
  - [[-2, -1], 1, Concat, [1]] # 18
  # 🔥 FPN第一个融合点 - 多尺度注意力
  - [-1, 1, MLKA, [256]] # 19-FPN多尺度融合
  - [-1, 3, RepC3, [256, 0.5]]  # 20, fpn_blocks.0
  - [-1, 1, Conv, [256, 1, 1]]   # 21, Y4, lateral_convs.1

  # 🔥 第二个CARAFE上采样
  - [-1, 1, CARAFE, [256]] # 22 (替换nn.Upsample)

  - [6, 1, Conv, [256, 1, 1, None, 1, 1, False]]  # 23 input_proj.0 (来自P3层)
  - [[-2, -1], 1, Concat, [1]]  # 24 cat backbone P3
  # 🔥 FPN第二个融合点 - 小目标关键层，强化注意力
  - [-1, 1, MLKA, [256]] # 25-小目标多尺度融合
  - [-1, 3, RepC3, [256, 0.5]]    # 26, fpn_blocks.1

  - [-1, 1, Conv, [256, 3, 2]]   # 27, downsample_convs.0
  - [[-1, 21], 1, Concat, [1]]  # 28 cat Y4
  # 🔥 PAN第一个融合点 - 新增下采样路径注意力
  - [-1, 1, MLKA, [256]] # 29-PAN多尺度融合
  - [-1, 3, RepC3, [256, 0.5]]    # 30, pan_blocks.0

  - [-1, 1, Conv, [256, 3, 2]]   # 31, downsample_convs.1
  - [[-1, 15], 1, Concat, [1]]  # 32 cat Y5
  # 🔥 PAN第二个融合点 - 新增底层注意力
  - [-1, 1, MLKA, [256]] # 33-PAN多尺度融合
  - [-1, 3, RepC3, [256, 0.5]]    # 34, pan_blocks.1

  # 检测层索引更新 - 使用增强后的特征层
  - [[26, 30, 34], 1, RTDETRDecoder, [nc, 256, 300, 4, 8, 3]]  # Detect(26,30,34)